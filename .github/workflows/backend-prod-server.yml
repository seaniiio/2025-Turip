name: 운영 환경 CI CD

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'backend/**'

jobs:
  build_and_test:
     runs-on: ubuntu-latest
     
     defaults:
      run:
        working-directory: backend
        
     steps: 
     - name: Checkout code
       uses: actions/checkout@v4

     - name: Set up Gradle
       uses: gradle/actions/setup-gradle@v3
             
     - name: Grant execute permission for gradlew
       run: chmod +x gradlew

     - name: Set up JDK 21
       uses: actions/setup-java@v4
       with:
         java-version: '21'
         distribution: 'temurin'
         cache: 'gradle'

     - name: Build JAR
       run: ./gradlew clean build -x test

     # - name: Run Unit Tests
     #   run: ./gradlew test
     
     - name: Login to Docker Hub
       uses: docker/login-action@v3
       with:
         username: ${{ secrets.DOCKERHUB_USERNAME }}
         password: ${{ secrets.DOCKERHUB_TOKEN }}

     - name: Build Docker image
       run: |
         IMAGE_NAME=seaoniiii/turip-server:prod-${{ github.sha }}
         echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_ENV
         docker buildx build --platform linux/arm64 -t $IMAGE_NAME -f Dockerfile.prod . --push

     - name: Push Docker image
       run: docker push ${{ env.IMAGE_NAME }}

  deploy:
    needs: build_and_test
    runs-on: [self-hosted, turip-test-prod]
    if: github.event_name == 'push'

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull and Run Docker container
        run: |
          IMAGE_NAME=seaoniiii/turip-server:prod-${{ github.sha }}
          docker pull $IMAGE_NAME

          # 기존 컨테이너 정리
          docker stop turip-prod || true && docker rm turip-prod || true

          # 새 컨테이너 실행
          docker run -d --name turip-prod -p 8080:8080 \
            -e "SPRING_PROFILES_ACTIVE=dev" \
            $IMAGE_NAME
